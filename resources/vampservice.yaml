gateways:
- shop-gateway
hosts:
- "1.2.3.4"
routes:
- protocol: http
  weights:
  - destination: shop-destination
    port: 8080
    version: subset1
    weight: 50
  - destination: shop-destination
    port: 8080
    version: subset2
    weight: 50
