gateways:
- shop-gateway
hosts:
- 35.241.185.250
routes:
- condition: uri prefix "/v1"
  rewrite: "/"
  protocol: http
  weights:
  - destination: shop-destination
    port: 8080
    version: subset1
    weight: 100
- condition: uri prefix "/v2"
  rewrite: "/"
  protocol: http
  weights:
  - destination: shop-destination
    port: 8080
    version: subset2
    weight: 100
