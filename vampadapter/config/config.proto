syntax = "proto3";

// config for vampadapter
package adapter.vampcadapter.config;

import "gogoproto/gogo.proto";

option go_package="config";

// config for vampadapter
message Params {
    // Path of the file to save the information about runtime requests.
    string file_path = 1;
}
